.menu
  .row.collapse
    .columns.small-5
      .logo
        =link_to "/" do
          - if @dark_logo
            =image_tag "dark_logo.png"
          - else
            =image_tag "logo.png"
    .columns.small-7
      .icon-nav
        =link_to "/blog", title: "Blog" do
          =fa_icon "rss"
        =link_to "/schedule", title: "Schedule A Phone Call" do
          =fa_icon "calendar"
        =link_to "/payments", title: "Make A Payment" do
          =fa_icon "money"
        =link_to "/contact", title: "Contact Us" do
          =fa_icon "envelope"
