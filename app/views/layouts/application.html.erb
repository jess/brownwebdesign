<!DOCTYPE html>
<html>
<%= render 'layouts/head' %>
<body class="<%= @body_class %>">
<%= render "layouts/menu" %>
<div class="row">
  <div class="columns">
    <%= yield %>
  </div>
</div>
</body>
</html>
